doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title= title

    // 🔹 Adding external styles and fonts for better compatibility
    link(rel="stylesheet", href="/styles.css")

    // 🔹 Space for page-specific CSS (optional)
    block styles

  body
    // 🔹 Include the header
    include ../partials/header.pug

    // 🔹 This is where the content of each page will be inserted
    block content

    // 🔹 Include the footer
    include ../partials/footer.pug

    // 🔹 Space for page-specific JavaScript (optional)
    block scripts

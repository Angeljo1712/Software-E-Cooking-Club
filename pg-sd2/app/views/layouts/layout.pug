doctype html
html(lang="en")
  head
    // 🔹 Set character encoding to UTF-8 for universal text support
    meta(charset="UTF-8")

    // 🔹 Ensure proper rendering on mobile devices with responsive design
    meta(name="viewport" content="width=device-width, initial-scale=1.0")

    // 🔹 Set the page title dynamically based on the `title` variable
    title= title

    // 🔹 Add external styles (CSS file) for styling the webpage
    link(rel="stylesheet", href="/styles.css")

    // 🔹 Block for additional CSS styles specific to individual pages (optional)
    block styles

  body
    // 🔹 Include the header (navigation bar or top section of the page)
    include ../partials/header.pug

    // 🔹 Main content block where each page's specific content will be inserted
    block content

    // 🔹 Include the footer (bottom section of the page)
    include ../partials/footer.pug

    // 🔹 Block for additional JavaScript files specific to individual pages (optional)
    block scripts

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title= title

    // 🔹 Agregamos más compatibilidad con estilos y fuentes externas
    link(rel="stylesheet", href="/styles.css")

    // 🔹 Espacio para agregar CSS específico por página (opcional)
    block styles

  body
    // 🔹 Incluir la cabecera (header)
    include ../partials/header.pug
    

    // 🔹 Aquí se insertará el contenido de cada página
    block content

    // 🔹 Incluir el pie de página (footer)
    include ../partials/footer.pug

    // 🔹 Espacio para agregar JavaScript específico por página (opcional)
    block scripts

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")

    // 🔹 Título dinámico desde cada vista
    title= title

    // 🔹 Estilos globales (si tienes más CSS en el futuro)
    link(rel="stylesheet", href="/css/login.css")

    // 🔹 Iconos de Font Awesome (para login buttons)
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css")

    // 🔹 CSS personalizado por vista (opcional)
    block styles

  body
    // 🔹 Header solo si la página lo necesita
    include ../partials/header.pug

    block content

    include ../partials/footer.pug

    block scripts
